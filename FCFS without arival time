
#include<bits/stdc++.h>
using namespace std;
int wt[100],bt[100],avwt=0,avtat=0,tat[100];
    int p,b;

void waitime(int bt[],int p,int wt[],int avwt)
{
    int temp=0;
    wt[0]=0;
    for(int i=1;i<p;i++)
    {
        wt[i]=0;
        for(int j=0;j<i;j++)
        {
            wt[i]=wt[i]+bt[j];
        }
        
    }
    // cout<< "Waiting time"<<endl;
    for(int i=0;i<p;i++)
    {
        avwt=wt[i]+avwt;
        temp=avwt/p;
        
        // cout<<wt[i]<<endl;
        
    }
    cout<< "Average time of Waiting time:-"<< temp<< endl;
}
void tempat(int bt[],int p,int wt[],int tat[] ,int avtat)
{
    int trmp;
    // cout<<"turnaround time"<<endl;
    for (int i=0;i<p;i++)
    
    {
        tat[i]=bt[i]+wt[i];
        // cout<<tat[i]<<endl;
        avtat=tat[i]+avtat;
        trmp=avtat/p;
        
        
    }
    cout<< "Average time of tat:-"<< trmp<< endl;
}
int main()

{
    
    cout<<"Enter the number of process "<< endl;
    cin>>p;
   
   
    cout<<"Enter the burst time of process"<< endl;
    for(int i=0;i<p;i++)
    {
        cin>>bt[i];
    }
    waitime(bt,p,wt,avwt);
    tempat(bt,p,wt,tat,avtat);
    cout<<"process "<<"bursttime "<<" waitingtime "<< "tat "<<endl;
    for(int i=0;i<p;i++)
    {
        cout<<"Process "<<i+1<<"      "<<bt[i]<<"      "<<wt[i]<<"       "<<tat[i]<<"     "<<endl;
    }
    
    return 0;
}
